# Copy to db.postgrenosql.env (mode 0600) and fill in. Never commit real credentials.
# Same PostgreSQL server as the relational backends; only the schema and binding differ.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=ycsb
DB_PWD=change-me
PG_MAINTENANCE_DB=postgres
