# Copy to db.postgresql_row.env, db.postgresql_textarray.env or db.postgresql_json.env (mode
# 0600), after the backend you run, and fill in. Never commit real credentials.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=ycsb
DB_PWD=change-me
PG_MAINTENANCE_DB=postgres
